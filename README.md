# stow -d ~/dotfiles -t ~ -S <package> --> symlink config 
# stow -d ~/dotfiles -t ~ -D <package> --> unlink config
# Dotfiles
## Stowed packages
1. bash
   - uses starship prompt if configuraiton is available
   - <ctl>+t initiates a recursive file search from the current path
   - <ctl>+r initiates a search of the commands in .bash_history
   - alias ll
   - alias r
2. ranger
   - contains 
3. starship
   - contains a theme toml for the starship prompt
